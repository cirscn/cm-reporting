/**
 * @file ui/checker/types.ts
 * @description 模块实现。
 */

// 说明：类型导出入口（聚合 core/rules 的类型）。
/**
 * 导出类型：CheckerSummary/PassedItem（用于 UI 展示）。
 */
export type { CheckerSummary, PassedItem } from '@core/rules/summary'
